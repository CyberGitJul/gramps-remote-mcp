-r requirements.txt
pytest>=8.0.0
pytest-cov>=5.0.0
ruff==0.15.22
pre-commit>=3.5.0
# The Stage-2 use-case catalog is one YAML per use case (tests/e2e/stage2/use_cases). It is
# only in the venv transitively today; CI installs from this file, so the Docker-free catalog
# tests would fail to import there without it.
PyYAML>=6.0
pip-audit>=2.7.0
